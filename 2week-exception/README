1. 我们在数据库操作的时候，比如 dao 层中当遇到一个 sql.ErrNoRows 的时候，是否应该 Wrap 这个 error，抛给上层。为什么，应该怎么做请写出代码？

我理解按业务需求, 一般我们做业务需求, 或者一个列表的数据是空的话就直接返回空数组, 没必要返回wrap一个error记录到日志, 但是如果获取数据是空的时候是一种异常情况, 那就需要wrap住他了, 假设例如一个列表, 点击详情页的时候,一般情况下这个详情页是不会空(因为空的话, 列表也应该不展示出来, 保证这个可能包含一些技术手段), 这个时候我是关注这个错误的, 那就要把他wrap住, 而记录到日志上, 方便系统监控业务异常.

ps: 我觉得这个问题是不是用文字描述一下会比写代码更加清晰, 写代码理解上是实现上的方法, 所以没有实现代码, 老师觉得有问题我也能补上